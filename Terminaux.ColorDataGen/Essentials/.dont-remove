This folder contains necessary shortcuts to reference the most minimum requirements to generate color data.

This is a placeholder text file because Git doesn't allow empty directories. Don't remove.
