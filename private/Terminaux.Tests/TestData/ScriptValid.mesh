set -set=$hell Hello
echo <$:hell>
set -set=$helol "Hello from $hell"
echo <$:helol>
input -set=$test Hello? 
echo <$:test>
choice -m -set=$test2 y/n "Found any bugs?"
echo <$:test2>
setrange -set=$bye "Goodbye from" me "AND this" is "EoflaOE!"
echo <$:bye[0]> <$:bye[1]> <$:bye[2]> <$:bye[3]> <$:bye[4]> '<$:bye[4]>'
if $test is string
|echo "It's a string"
if $test is integer
|echo "It's an integer"
